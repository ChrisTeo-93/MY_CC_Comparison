{
  "name": "@kadcompare/core",
  "version": "0.1.0",
  "private": true,
  "description": "Shared domain model, recommendation engine, and persona logic for KadCompare — consumed by both the web app and the mobile app. Pure TypeScript, no framework dependencies.",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "5.5.3"
  }
}
